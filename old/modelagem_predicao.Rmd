---
title: "R Notebook"
output: html_notebook
---
```{r}
library(plotly)
library(scales)
library(caret)
library(ISLR)
library(splines)
```

```{r}
data.train <- df
# data.test  <- data.all[-train.ids,]
```

```{r}

# TODO - Calcular distancia entre ponto de partida/chegada e o ponto turistico
# Usar a funcao de distancia euclidiana

# Referencia para regressao linear com multiplas variaveis
# https://stackoverflow.com/questions/5251507/how-to-succinctly-write-a-formula-with-many-variables-from-a-data-frame
head(data.train$age)
trip_duration_BS = bs(data.train[,c("hour_quarter", "pickup_longitude", "pickup_latitude")], df = 3)
head(trip_duration_BS)
```

```{r}
lm1 <- lm( data.train$trip_duration ~ age_BS)
plot(data.train$age, data.train$wage)
predito = predict(lm1, newdata=data.train) #Prediz os pontos
points(data.train$age, predito, col='red', pch=19)
```

